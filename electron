#!/bin/bash
PROJECT_DIR="__PROJECT_DIR__"
if [ "$PROJECT_DIR" = "__PROJECT_DIR__" ]; then
  PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
fi
cd "$PROJECT_DIR" || exit 1
export URL="$1"
echo "$URL"
npm start "$1"
